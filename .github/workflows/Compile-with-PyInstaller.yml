name: Build with PyInstaller

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set PYTHONIOENCODING
        run: |
          $env:PYTHONIOENCODING = 'utf-8'

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile with PyInstaller
        run: |
          pip install pyinstaller
          pyinstaller easy_slides.spec

      - name: Compress output folder
        run: |
          Compress-Archive -Path './dist/easy_slides/*' -DestinationPath './dist/easy_slides.zip'

      - name: Upload zip to release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./dist/easy_slides.zip
          asset_name: easy_slides.zip
          asset_content_type: application/zip